/**
 * policy-adjustment-request controller
 */

import { factories } from "@strapi/strapi";

export default factories.createCoreController(
  "api::policy-adjustment-request.policy-adjustment-request",
);
